# Start
shellscript to generate a standard express (JS/TS) project structure.
## Usage

```bash
chmod +x run.sh

# JavaScript (default)
./run.sh my-app

# TypeScript
./run.sh my-app --ts
cd my-app
npm install
npm run dev
```
